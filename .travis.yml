sudo: false
language: cpp

os:
  - linux

notifications:
  email: false

addons:
  apt:
    packages:
      - openssh-client
      - git

install:
  - cp rsa ~/.ssh/id_rsa
  - chmod 600 ~/.ssh/id_rsa
  - cd /tmp
  - git clone git@github.com:amtlib-dot-dll/vscode-build-travis.git
  - cd vscode-build-travis
  - git checkout out

script:
  - find | tee out.txt
  - git add .
  - git commit -a -m 'hahaha'

after_success:
  - git push 
